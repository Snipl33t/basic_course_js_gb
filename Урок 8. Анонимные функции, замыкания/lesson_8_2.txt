// Если я правильно понимаю, что мы рассматриваем данные функции отдельно (каждые куски существуют отдельно от других), то решение следующее

1.
if (!("a" in window)) {
    var a = 1;
}
alert(a);

// Выведет 1 если "a" есть в window, иначе undefined

2.
var b = function a(x) {
    x && a(--x);
};
alert(a);

// Скорее всего выведет ошибку, т.к. переменная "а" нигде не определенна, а то что написанно в "b" находится в ее лексическом окружении (если я правильно это понимаю)

3.
function a(x) {
    return x * 2;
}
var a;
alert(a);

// Выведет тело функции, т.к. "a" определена как функция и никакого аргументы ей передано не было.

4.
function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);

// Выведет 10, т.к. в функции б 2 аргумент выставляется равным 10, тоесть а = 10.

5.
function a() {
    alert(this);
}
a.call(null);

// Прочитал на сайте https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Function/call , что при вызове налл выведет глобальный объект, т.е. наверное window